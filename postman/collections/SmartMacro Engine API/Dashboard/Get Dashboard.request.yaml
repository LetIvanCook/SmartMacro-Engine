$kind: http-request
name: Get Dashboard
method: GET
url: '{{baseUrl}}/api/dashboard/{{userId}}/dashboard'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
